const config = {
  name: "mine",
  description: "Dig materials to sell and die doing it forever",
  version: "1",
  credits: "Renz"
}

async function onCall({data, message}) {
  
}

export default {
  config,
  onCall
}